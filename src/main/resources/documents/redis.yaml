repositories:
# Stable repo of official helm charts
- name: "stable"
  url: "https://kubernetes-charts.storage.googleapis.com"

releases:
  - name: 'redis'
    namespace: 'eurinome'
    labels:
     chart: 'redis'
     repo: 'stable'
     component: 'redis'
     namespace: 'eurinome'
    chart: 'stable/redis'
    version: '~5.1.6'
    values:

